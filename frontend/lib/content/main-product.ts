/**
 * Single source for the main PDP path segment: /products/<slug>
 * Keep middleware/routing imports limited to this file (edge-safe, no API client).
 */
export const MAIN_PRODUCT_SLUG = "blueKSA" as const;
